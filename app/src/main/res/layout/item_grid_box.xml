<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    style="?listItem">

  <Button
      style="?attr/borderlessButtonStyle"
      android:textAllCaps="false"
      android:id="@+id/text_button"
      android:gravity="center"
      tools:text="1"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:paddingLeft="@dimen/scripture_picker_box_padding"
      android:paddingRight="@dimen/scripture_picker_box_padding"
      android:paddingTop="@dimen/scripture_picker_box_padding"
      android:paddingBottom="@dimen/scripture_picker_box_padding"/>

</FrameLayout>
